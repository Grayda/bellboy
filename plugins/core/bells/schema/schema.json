{
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "minLength": 2,
        "title": "Name",
        "description": "Name or alias",
        "default": "{{ bell.Name }}"
      },
      "title": {
        "type": "string",
        "enum": ["dr", "jr", "sir", "mrs", "mr", "NaN", "dj"]
      }
    }

  },
  "form": [
    "*",
    {
      "type": "submit",
      "title": "Save"
    }
  ]
}
