var express = require('express');
var router = express.Router();

module.exports = function(passport, bellboy) {
  //
  // router.get('/add.ejs', function(req, res) {
  //   mw.toInspect = {
  // 	firstname: "Joe",
  // 	surname: "Bloggs"
  // };
  // derp = mw.buildWidgets();
  // console.dir(derp)
  //
  // });
  //

  return router;
}
